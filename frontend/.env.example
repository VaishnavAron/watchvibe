# WatchVibe Frontend Environment Configuration Template
# Duplicate this file as '.env' in frontend directory.
# If running locally with Vite proxy to port 4000, leave VITE_API_URL empty or point to backend.
VITE_API_URL=http://localhost:4000
